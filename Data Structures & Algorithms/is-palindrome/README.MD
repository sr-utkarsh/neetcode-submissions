New learnings : 
s.replaceAll("[^a-zA-Z0-9]", ""); //used to remove all special characters and spaces from string

String s2 = s1.toLowerCase(); //converting string to lowercase

char[] array = s2.toCharArray(); //converting string to char array
